namespace Input {
    /// <summary>
    /// Define the type of device that is currently being used
    /// </summary>
    public enum EDeviceType {
        GAMEPAD,
        KEYBOARD,
        JOYSTICK,
    }
}