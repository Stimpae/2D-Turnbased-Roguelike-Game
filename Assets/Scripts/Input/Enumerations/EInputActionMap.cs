namespace Input {
    /// <summary>
    /// Update the EInputActionMap enum to match
    /// the Input Action Maps you have defined in your project.
    /// </summary>
    public enum EInputActionMap {
        DEFAULT,
        UI,
        PLAYER,
    }
}